{
  "name": "auth0-react-router",
  "version": "1.0.0-beta.1",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "docs": "typedoc",
    "prepare": "husky",
    "postinstall": "test -d packages/auth0-react-router/dist || npm run build"
  },
  "author": "",
  "license": "ISC",
  "description": "A React Router integration for Auth0's React SDK",
  "keywords": [
    "auth0",
    "react",
    "react-router",
    "authentication",
    "authorization"
  ],
  "packageManager": "npm@11.7.0",
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "husky": "^9.1.7",
    "turbo": "^2.7.3",
    "typedoc": "^0.28.15"
  },
  "dependencies": {
    "@auth0/auth0-server-js": "^1.2.0",
    "react-router": "^7.11.0"
  }
}